import React from 'react';
import CategoryContainer from '../../components/categoryContainer';

const Mapping = () => {
  return <CategoryContainer category={'mapping'} />;
};

export default Mapping;
