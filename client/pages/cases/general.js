import React from 'react';
import CategoryContainer from '../../components/categoryContainer';

const General = () => {
  return <CategoryContainer category={'general'} />;
};

export default General;
