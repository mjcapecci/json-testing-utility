import React from 'react';
import CategoryContainer from '../../components/categoryContainer';

const Landing = () => {
  return <CategoryContainer category={'landing'} />;
};

export default Landing;
