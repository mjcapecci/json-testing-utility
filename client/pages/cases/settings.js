import React from 'react';
import CategoryContainer from '../../components/categoryContainer';

const Settings = () => {
  return <CategoryContainer category={'settings'} />;
};

export default Settings;
